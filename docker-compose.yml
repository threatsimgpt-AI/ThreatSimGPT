# ================================
# ThreatSimGPT - Complete Docker Stack
# ================================
# Production-ready containerized deployment
# Services: PostgreSQL, Redis, Ollama, ThreatSimGPT API, Prometheus, Grafana

services:
  # ================================
  # PostgreSQL Database
  # ================================
  postgres:
    image: postgres:16-alpine
    container_name: threatsimgpt-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-threatsimgpt}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-threatsimgpt_secure_pass_2024}
      POSTGRES_DB: ${POSTGRES_DB:-threatsimgpt}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d
    ports:
      - "5433:5432"
    networks:
      - threatsimgpt-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-threatsimgpt}"]
      interval: 10s
      timeout: 5s
      retries: 5
    labels:
      com.threatsimgpt.service: "database"
      com.threatsimgpt.description: "PostgreSQL database for persistent storage"

  # ================================
  # Redis Cache
  # ================================
  redis:
    image: redis:7-alpine
    container_name: threatsimgpt-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports:
      - "6380:6379"
    networks:
      - threatsimgpt-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    labels:
      com.threatsimgpt.service: "cache"
      com.threatsimgpt.description: "Redis for caching and session management"

  # ================================
  # Ollama - Local LLM Server
  # ================================
  ollama:
    image: ollama/ollama:latest
    container_name: threatsimgpt-ollama
    restart: unless-stopped
    volumes:
      - ollama_models:/root/.ollama
    ports:
      - "11434:11434"
    networks:
      - threatsimgpt-network
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_ORIGINS=*
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    labels:
      com.threatsimgpt.service: "llm"
      com.threatsimgpt.description: "Local LLM server with Ollama"
    deploy:
      resources:
        limits:
          memory: 8G
        reservations:
          memory: 4G

  # ================================
  # ThreatSimGPT API Server
  # ================================
  threatsimgpt-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: threatsimgpt-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_started
    environment:
      # Database
      - DATABASE_URL=postgresql://${POSTGRES_USER:-threatsimgpt}:${POSTGRES_PASSWORD:-threatsimgpt_secure_pass_2024}@postgres:5432/${POSTGRES_DB:-threatsimgpt}
      # Redis
      - REDIS_URL=redis://redis:6379/0
      # LLM Providers
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OLLAMA_BASE_URL=http://ollama:11434
      - DEFAULT_LLM_PROVIDER=${DEFAULT_LLM_PROVIDER:-openrouter}
      # Application
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - SECRET_KEY=${SECRET_KEY}
      - ENVIRONMENT=${ENVIRONMENT:-development}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - DEBUG=${DEBUG:-false}
      # Python
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ./threatsimgpt:/app/threatsimgpt:ro
      - ./templates:/app/templates:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - ./generated_content:/app/generated_content
      - ./reports:/app/reports
    ports:
      - "8001:8000"
    networks:
      - threatsimgpt-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      com.threatsimgpt.service: "api"
      com.threatsimgpt.description: "ThreatSimGPT main API server"

  # ================================
  # Prometheus - Metrics Collection
  # ================================
  prometheus:
    image: prom/prometheus:latest
    container_name: threatsimgpt-prometheus
    restart: unless-stopped
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    ports:
      - "9091:9090"
    networks:
      - threatsimgpt-network
    depends_on:
      - threatsimgpt-api
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      com.threatsimgpt.service: "monitoring"
      com.threatsimgpt.description: "Prometheus metrics collection"

  # ================================
  # Grafana - Metrics Visualization
  # ================================
  grafana:
    image: grafana/grafana:latest
    container_name: threatsimgpt-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin_change_this}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3000
      - GF_INSTALL_PLUGINS=
    volumes:
      - grafana_data:/var/lib/grafana
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3001:3000"
    networks:
      - threatsimgpt-network
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      com.threatsimgpt.service: "monitoring"
      com.threatsimgpt.description: "Grafana metrics dashboard"

  # ================================
  # PgAdmin - Database Management (Optional)
  # ================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: threatsimgpt-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@threatsimgpt.local
      PGADMIN_DEFAULT_PASSWORD: admin_change_this
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    ports:
      - "5050:80"
    networks:
      - threatsimgpt-network
    depends_on:
      - postgres
    profiles:
      - tools
    labels:
      com.threatsimgpt.service: "tools"
      com.threatsimgpt.description: "PgAdmin database management interface"

# ================================
# Networks
# ================================
networks:
  threatsimgpt-network:
    driver: bridge
    name: threatsimgpt-network
    labels:
      com.threatsimgpt.network: "main"

# ================================
# Volumes
# ================================
volumes:
  postgres_data:
    name: threatsimgpt-postgres-data
    labels:
      com.threatsimgpt.volume: "database"
  
  redis_data:
    name: threatsimgpt-redis-data
    labels:
      com.threatsimgpt.volume: "cache"
  
  ollama_models:
    name: threatsimgpt-ollama-models
    labels:
      com.threatsimgpt.volume: "models"
  
  prometheus_data:
    name: threatsimgpt-prometheus-data
    labels:
      com.threatsimgpt.volume: "metrics"
  
  grafana_data:
    name: threatsimgpt-grafana-data
    labels:
      com.threatsimgpt.volume: "dashboards"
  
  pgadmin_data:
    name: threatsimgpt-pgadmin-data
    labels:
      com.threatsimgpt.volume: "tools"
